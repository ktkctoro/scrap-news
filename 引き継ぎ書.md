# 引き継ぎ書 — スクラップ今日

このファイルは、**新しいセッションでこの続きを作るとき**に最初に読むもの。
何ができているか、どこでハマったか、次に何をするかを書いてある。

仕様そのものは `CLAUDE.md` にある。こちらは「経緯と注意点」。

---

## 0. 新しいセッションの始め方

これまでの作業は、別のプロジェクト（vlog-maker）のフォルダで開いたセッションから
このフォルダを触る形になっていた。そのため vlog-maker 側の規約が読み込まれてしまう。
**これからは必ず `scrap-news` フォルダでセッションを開くこと。**

Claude Code のターミナルで:

```bash
cd "$env:USERPROFILE\scrap-news"
```

デスクトップアプリなら、フォルダ選択で `scrap-news` を開く。
そうすればこのフォルダの `CLAUDE.md` が自動で読み込まれる。

最初の一言はこれで足りる:

> `引き継ぎ書.md` と `CLAUDE.md` を読んで、続きをやってください。

---

## 1. これは何か

スクラップ・非鉄金属の買取業（小林商会）の事業主が、
**朝いちにスマホで、自分の商売に効く情報だけを1分で確認する**ための仕組み。

- **見るところ: https://ktkctoro.github.io/scrap-news/**
- 毎朝4時30分（日本時間）にGitHubのサーバーが勝手に動いて中身を作り直す。
  **パソコンの電源は関係ない。**
- リポジトリは公開（public）。建値も買取価格も、URLを知っていれば誰でも見られる。
  これは本人が了解済み（「全部公開でよい」）。

## 2. いま画面に載っているもの

上のタブは2枚。

**「けさ」タブ**

| 欄 | 中身 |
|---|---|
| けさの3行 | AIが記事全部を読んで書いた3行のまとめ |
| 月次レポート | 毎月1日に書き換わる。5段落・緑ラベル。たたんである |
| 週次レポート | 毎週月曜に書き換わる。4段落・青ラベル。たたんである |
| 建値タイル | 銅建値・鉄スクラップ・金・銀・ドル円・日経平均・NYダウ・ビットコイン |
| 基板の買取価格 | K&Yシステムの6品目 |
| 分類チップ | すべて／相場／法規制／業界／AI・DC／ビットコイン |
| ニュース欄 | 1つの窓の中でスクロール。同じ出来事は1本にまとめて「ほかN社」 |
| AIの朝3本 | The Last Three Minutes の当日3本 |
| AIに聞く | 質問を入れるとClaudeのチャットが質問入りで開く |

**「くらべる」タブ** — 同業8社の買取価格の横並び

- 中は「スクラップ／基板」の2枚。
- 基板側はさらに内タブ4枚（クラス／CPU・IC／PC部品／機器）で、高い順に並ぶ。
- いちばん高い先を緑で強調し、最安との差額も出す。
- 2社以上が載せている品目だけ表示する。

建値タイル・貴金属の行・基板の行を**タッチすると3ヶ月チャート**がポップアップで出る。

## 3. 作った順番（経緯）

だいたいこの順で足していった。あとの機能ほど、前の機能の作りに乗っている。

1. **ニュースのふるい分け** — 最初は203件も出て使い物にならなかった。
   `RELEVANT`（これが無ければ捨てる）＋ `NG_WORDS` ＋ `NG_SOURCES` ＋ `CONDITIONAL`
   の4段構えにして24件まで絞った
2. **毎朝の自動実行を GitHub Actions + GitHub Pages に置いた** —
   最初はWindowsのタスクスケジューラで作りかけたが、本人の望みは
   「**携帯で見られること**」だった。パソコンが動いていないと見られない方式では
   意味がないので全部やり直した
3. **価格の自動取得** — 手入力をやめたいという話から。銅建値（JX金属）、
   鉄スクラップ（東京製鐵のPDF）、金・銀（田中貴金属）、ドル円、
   ビットコイン、日経平均、NYダウ
4. **基板の買取価格**（K&Yシステム）6品目
5. **同業8社の価格くらべ** — きっかけは「価格表サイトへのリンクを作って」だったが、
   聞いたら「**複数あるのは比較検討したいからです**」。
   リンク集ではなく比較表が本当に欲しかったもの。ここは読み違えた
6. **デザインを3色に整理**（青・緑・朱）、明るめの色合いに
7. **ビットコインの話題**を追加、専用のチップも
8. **重複記事をまとめる**
9. **3ヶ月チャートのポップアップ**
10. **AIによる二次ふるい分け** — 「削除とニュースピックアップの精度を上げて」
11. **The Last Three Minutes** を取り込む
12. **週次レポート**（毎週月曜）
13. **月次レポート**（毎月1日）← いまここ

## 4. ハマったところ（同じ穴に落ちないために）

### Windows まわり

- **`python` は使わない。`py` を使う。** このパソコンの `python` は
  Microsoft Store のダミーに繋がっていて動かない
- **`strftime("%-m/%-d")` は Windows で落ちる。** `%-` はLinux専用。
  文字列を自分で組み立てること
- **PowerShell 5.1 の `Get-Content` は cp932 で読む。**
  一度これで `CLAUDE.md` を読み書きして**全部文字化けさせた**。
  git から戻して直した。UTF-8のファイルをPowerShellで加工しないこと
- **PowerShell のヒアストリングやクォートは何度も失敗した。**
  複雑な処理は作業用の `.py` ファイルを書いて `py` で実行するほうが速い
- 出力の文字化けには `PYTHONUTF8=1`

### git / GitHub Actions

- **`git pull --rebase -X ours` を使ってはいけない。**
  rebase 中の `ours` は**リモート側**を指す。これで、その朝に作った
  `data.json`（週次レポート入り）を丸ごと捨てた。
  正しくは `git fetch origin main` → `git reset --soft origin/main`。
  こうすると作ったファイルはそのまま残る
- `run_collect.bat` と `毎朝の自動実行.md` は **gitignore 済み**。
  中に個人のフォルダ名（本名）が入っていて、リポジトリは公開だから
- ワークフローは手で回せる: `gh workflow run collect.yml` → `gh run watch`。
  レポートを臨時で書かせたいときは
  `gh workflow run collect.yml -f force_weekly=true`（月次は `force_monthly`）

### 取り込みの精度

- **Googleニュースは提供元を「nikkei.com」のようなドメイン形で返すことがある。**
  「日本経済新聞」だけ弾いても素通りする。**両方の書き方を登録**し、
  照合は小文字化してから
- **話題の多い検索語は1つで欄を埋め尽くす。** ビットコインが102件中41件を
  占めた。`QUERIES` の第4要素で検索語ごとに上限をかけている（btcは5本）
- **AIの二次ふるい分けがビットコイン記事を全部消した。**
  プロンプトに「外すな」と書くだけでは足りず、**コード側でも `cat == "btc"` を
  除外**する固い歯止めを入れてある。さらに「捨てるのは全体の3割まで」の上限も
- **同じ出来事の判定は、数字が一致しないときは厳しくする。**
  そうしないと「銅建値240万円」と「銅建値234万円」（別の日の話）が
  1本にまとめられてしまう

### 同業比較の品目合わせ

会社ごとに品名の書き方がぜんぜん違うので、`dealers.py` の `COMPARE` に
**各社での言い方を手で並べてある**。ここで踏んだ地雷:

- 「給湯器」が大畑の「赤釜 1,900円」を拾った → ガス給湯器を先に置いて解決
- 「バッテリー」がハイブリッド車 2,000円を拾った → 「自動車用 鉛バッテリー」に
- **「銅」「鉄」「アルミ」のような短い語は、部分一致だと別品目を拾う。**
  → 呼び名の先頭に **`=`** を付けると完全一致のみになる仕組みを入れた
- キバセンは品名に空白が入らない。空白入りで書くと1件も当たらない
- **光学ドライブと雑基板は、わざと比較から外している。**
  会社によって中身が別物なので、並べると誤解を生む

## 5. 守っていること

- **追加ライブラリを入れない。** Python標準ライブラリだけ。
  `pip install` が要る変更は、**先に理由を説明して確認を取る**
- **1つの取得元がこけても、他は出す。** 価格・比較・記事・レポート・
  AIの朝3本、すべて**前回の値を持ち越す**作りにしてある。
  ニュースが取れなかった朝は前回の記事を残し、画面にその旨を出す（`items_stale`）
- **画面の言葉・ログ・エラーは全部日本語。** 読む人はターミナルに詳しくない
- **分類は5つから増やさない。** チップが増えると朝いちに読めなくなる
- **APIの鍵はチャットに貼らせない。** GitHub Secrets の `ANTHROPIC_API_KEY` にだけ入れる

## 6. 次にやること

1. 数日ぶん動かして、まだ混ざる関係ない記事があれば `NG_WORDS` / `NG_SOURCES` を足す。
   逆に、拾ってほしいのに落ちている記事があれば `RELEVANT` に語を足す
2. **アルミ二次地金の自動取得先をさがす。** 四半期ごとの交渉で決まる相場で、
   日々の数字は有料紙しかない。見つかるまでは `manual.json` に手で書く方式
3. Googleニュースの無料フィードは新着が遅れがちで、当日の記事が少ない日がある。
   別の取得元を足すかどうかは要検討

## 7. 動作確認のしかた

パソコンで試すとき:

```bash
py collect.py
py check_data.py
py build_page.py
```

`build_page.py` が作る `携帯用.html` はダブルクリックで開ける。
`index.html` を直接開くと `data.json` を読めない（ブラウザの制限）ので、
そちらを見たいときは `py -m http.server 8000` を通すこと。

GitHub側で試すとき:

```bash
gh workflow run collect.yml
gh run watch
```

---

*最終更新: 2026-09-08 / 最新コミット: `4f98ac0 月次レポートを追加する`*
