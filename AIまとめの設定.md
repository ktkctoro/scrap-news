# 「けさの3行」AIまとめの設定（一度だけ）

毎朝の収集のあと、Claude(AI)がその日の相場と見出しから
**3行のまとめ**を書いて、ページのいちばん上に載せる仕組みです。

動かすには **APIキー(鍵)** が1つ必要です。取得は10分ほど、
費用は使った分だけの従量制で、この使い方なら**月100〜200円程度**です。

> **大事な注意**: 鍵は「お金を使える合鍵」です。
> 誰にも見せない・どこにも貼らない(チャットにも)。
> 置き場所は下で説明する GitHub の金庫(Secrets)だけです。

---

## 手順1: Claudeの鍵を作る

1. パソコンのブラウザで **https://console.anthropic.com/** を開く
2. 「Continue with Google」で、いつもの Gmail アカウントでログイン
3. 左メニューの **Billing**(支払い)でクレジットカードを登録し、
   最少額(5ドル)のクレジットを購入する
   ※使った分だけ減る前払い式。月100〜200円のペースなら5ドルで半年〜1年もちます
4. 左メニューの **API Keys** → **Create Key** → 名前は `scrap-news` などにして作成
5. 表示された `sk-ant-` で始まる長い文字列が鍵です。**この画面でしか見られない**ので、
   いったんメモ帳などにコピーしておく(手順2が終わったら消す)

## 手順2: 鍵をGitHubの金庫に入れる

1. ブラウザで **https://github.com/ktkctoro/scrap-news/settings/secrets/actions** を開く
2. **New repository secret** を押す
3. Name に `ANTHROPIC_API_KEY` と入れる(この名前のとおりに)
4. Secret に手順1の鍵を貼り付けて **Add secret**
5. メモ帳に残した鍵を消す

これで完了です。金庫に入れた鍵は、GitHubのサーバーの中でだけ使われ、
ページや `data.json` には一切出ません。

## 手順3: 試しに動かす

パソコンのターミナルで:

```bash
cd C:\Users\小林猛\scrap-news; gh workflow run collect.yml
```

3〜4分後に https://ktkctoro.github.io/scrap-news/ を開くと、
いちばん上に「**けさの3行（AIのまとめ）**」が出ています。

以後は毎朝4:30の自動収集のたびに、その日の3行が新しく書かれます。

---

## こまったとき

- **まとめの欄が出ない** → 鍵の名前が `ANTHROPIC_API_KEY` になっているか、
  クレジット残高が残っているかを確認。まとめが失敗しても記事と価格は普通に出ます
- **やめたいとき** → GitHubの同じ画面で secret を削除するだけ。
  ほかの機能はそのまま動き続けます
- **残高の確認** → console.anthropic.com の Billing でいつでも見られます
